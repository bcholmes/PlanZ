import React from "react";

export const AutoSchedulerIntroduction: React.FC<{}> = () => {

    return (<div>
        <p>The auto-scheduler is a tool that tries to find the best schedule based on
            people's sign-up preferences, availability and room capacities.</p>

        <p>At the moment, the following schedule slots are available:</p>

    </div>)
}